node_modules/
.env
.ingest-state.json
logs/
eval/runs/
.claude/

# Qdrant's NATIVE default storage path when qdrant.exe is launched from the repo root.
# NOTE: this is ./storage, NOT ./qdrant_storage — the tracked qdrant_storage/ is a stale
# 2026-08-11 database and is NOT what the running instance reads (verified 2026-09-10).
storage/
